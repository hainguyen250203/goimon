// Scrollbar mảnh, theo màu theme — dùng cho các vùng cuộn lồng bên trong
// Dialog (item list, table picker...) thay vì thanh cuộn OS mặc định to bản.
export const thinScrollbar = {
  scrollbarWidth: "thin",
  scrollbarColor: "var(--chakra-colors-border) transparent",
} as const;
